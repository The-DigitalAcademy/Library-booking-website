-- CREATE TABLE users (
--     users_id SERIAL PRIMARY KEY,
--     username VARCHAR(50) UNIQUE NOT NULL,
--     email VARCHAR(255) UNIQUE NOT NULL,
--     password VARCHAR(50) NOT NULL
-- )
-- drop table bookings cascade
-- CREATE  TABLE if not EXISTS bookings (
--     booking_id SERIAL PRIMARY KEY,
--     id integer,
--     users_id INT,
--     authors_id INT,
--     booking_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
--     return_date DATE,
--     FOREIGN KEY(id) REFERENCES books(id),
--     FOREIGN KEY(users_id) REFERENCES users(users_id),
--     FOREIGN KEY(authors_id) REFERENCES authors(authors_id)
-- )
-- ALTER TABLE bookings
-- ADD COLUMN  status VARCHAR(50) DEFAULT 'Pending'
-- ALTER TABLE bookings
-- ADD CONSTRAINT fk_book_id FOREIGN KEY (book_id)
-- REFERENCES books(book_id);
-- ALTER TABLE bookings
-- ADD COLUMN book_id INT;

ALTER TABLE users
ALTER COLUMN password TYPE VARCHAR(255)

-- CREATE TABLE books (
--      id SERIAL PRIMARY KEY,
--      book_id TEXT VARCHAR, 
--      category_id INT,
--      title VARCHAR(255) NOT NULL,
--      authors_id INT,
--      genre TEXT,
--      availability BOOLEAN DEFAULT TRUE,
--      published_date TEXT,
--      FOREIGN KEY (category_id) REFERENCEs categories(category_id),
--      FOREIGN KEY (authors_id) REFERENCES authors(authors_id)
-- )

-- ALTER TABLE books
-- ALTER COLUMN book_id TYPE VARCHAR(255);

-- ALTER TABLE bookings
-- ALTER add COLUMN book_id TYPE VARCHAR(255);

-- ALTER TABLE authors ADD CONSTRAINT unique_author UNIQUE (name);
-- SELECT COUNT(*) FROM books;


-- CREATE TABLE authors(
--     authors_id SERIAL PRIMARY KEY,
--     name VARCHAR(255)
-- )
-- ALTER TABLE authors
-- RENAME COLUMN name TO authors_name

-- CREATE TABLE categories (
--     category_id SERIAL PRIMARY KEY,
--     genre_name VARCHAR(255) NOT NULL,
--     description VARCHAR
-- )
-- ALTER TABLE categories
-- RENAME genre_name TO category_name
-- ALTER TABLE categories
-- DROP COLUMN description
-- ALTER TABLE books
-- ADD COLUMN description VARCHAR
-- ALTER TABLE books
-- ALTER COLUMN published_date TYPE DATE